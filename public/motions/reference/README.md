Synra reference VRMA parity slot
================================

Place a known-good VRMA file that plays correctly in VRoid Hub / VRoid Playground here:

reference_full_body_test.vrma

The NodeSparkHub runtime intentionally does not use generated draft clips as the reference.
Use the developer control "Play Reference Full-Body VRMA" or run:

window.__synraPlayReferenceMotion?.("reference_full_body_test")

